/*

  In this file all the "helper" functions will be made.
  Helper functions are reusable functions that we use all around the app.

  Example:

  export const displayDate = date111 => {
    Moment.locale('nl');
    return Moment(date).format('DD/MM/YYYY, HH:mm:ss');
  }

*/
